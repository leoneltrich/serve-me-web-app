# The central API prefix for backend routes (Relative by default)
PUBLIC_API_BASE_URL=/api/v1

# The actual backend destination (used by SvelteKit proxy and during build)
BACKEND_API_URL=http://localhost:2000
