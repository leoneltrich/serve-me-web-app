services:
  app:
    build: .
    ports:
      - "5000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
      - BACKEND_API_URL=http://host.docker.internal:2000
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
